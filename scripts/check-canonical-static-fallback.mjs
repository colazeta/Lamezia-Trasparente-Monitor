#!/usr/bin/env node
// Keep the established entrypoint, but test the exact emitted artifact.
// Schema validation and worker checks run together without rewriting data.
import "./check-v0-static-fallback.mjs";
