# Canonicalisation resumption — 19 September 2026

Related to #771 and PR #1214, which includes predecessor #1211. This is a
resumption of the bounded demographic migration and estate audit. It does not
certify completion of all domains or a production cutover.

## Reconciled code

The candidate was merged with main commit
`4a0ce9a74a46e9236fe0be16ee556ec86f48b8fa` without conflicts. Existing municipal
source bytes, public-data restrictions, unknown reference periods and the
separation from ISTAT series are preserved. No new schema migration is required
by this continuation.

The three municipal datasets retain the reviewed import/read contract: exact
source bytes and hashes, 50 source rows, 69 typed observations at the initial
fixture checkpoint, idempotent reruns, failed-import rollback, read-only public
queries and release-pinned downloads. These are candidate/test expectations,
not current production counts.

## Review coverage correction

The earlier audit linked every file under a registered asset directory to its
bounded review. Since the 10 September checkpoint, files in those directories
have been added and modified. A directory match cannot extend a past review to
those bytes.

The new review baseline preserves the 120 file receipts in
`LameziaTrasparente_canonicalisation_audit_1214.json`, whose SHA-256 is
`db88ea072fe2f55daa8309463dd058eb5a240660866c8325c463fd78eb4e9450` and whose
source commit is `1bb355a1c4d91d03b04d61bb4d55592fd09f30b8`.

The current audit distinguishes directory ownership from review validity.
Only matching bytes and matching asset ownership retain the original bounded
review link. New files, modified content and changed ownership remain
unreviewed with a null semantic status and an explicit reason. Invalid receipts
fail closed. A missing old file is reported separately; absence does not
establish a justified deprecation. The baseline is not refreshed merely to make
the current counts look better.

Counts are generated by `pnpm run architecture:canonicalisation`. Files, groups,
source rows, observations and production gates remain separate denominators.

On the reconciled candidate the executable audit reports:

| Measure | Count |
| --- | ---: |
| Discovered files | 1,210 |
| Files whose bytes and ownership still match the bounded review | 87 |
| Unreviewed or requiring renewed review | 1,123 |
| Files located in registered asset groups, regardless of review validity | 286 |
| Files changed since their bounded review | 32 |
| New files within registered asset locations | 167 |
| Reviewed asset groups | 19 |
| Groups certified canonical and consumed in production | 0 |

One historical review receipt has no current file. The categories explaining
review validity must not be added to the total again. These counts describe the
candidate at the stated main checkpoint; subsequent content commits may change
them and require a fresh audit.

## User-facing behaviour

Loading, unavailability and cached-data notices now explain the state of the
demographic data without requiring users to understand database implementation.
Unavailable data still does not imply zero, cached responses retain their
notice and downloads remain pinned to the displayed release.

## Validation executed locally

- `corepack pnpm run typecheck`: passed for the complete workspace.
- `corepack pnpm run build`: passed for the complete workspace, including API,
  public web app, mobile and worker. Existing bundle-size/sourcemap warnings
  remain; they were not converted into passing functional assertions.
- Database suite: 88 tests passed; the migration safety check passed. This
  environment refused the tsx CLI's IPC socket, so the exact same test files
  were executed with `node --import tsx --test`, and the migration check with
  `node --import tsx ./src/checkMigrations.ts`.
- Public/admin API tests: 20 passed in two files.
- Affected demographic/admin/source-health frontend tests: 30 passed in seven
  files, including loading, unavailable data, cached responses and pinned links.
- Canonicalisation ledger tests: six passed, including new/changed file review
  invalidation and malformed receipt rejection.
- Executable canonicalisation audit: no errors. Structural architecture audit:
  current, 75 tables across 47 modules.

The separate PostgreSQL 18 integration workflow and full remote CI must be read
on the updated PR commit; previous 10 September receipts do not certify this
continuation. No live visual sign-off or production import is claimed.

## Operational boundary

The public source-snapshot readiness URL could not be verified from this
session: the web read was unavailable and an independent HTTP request timed
out. This does not establish that the service is down. It also provides no
evidence of a completed production import.

The user subsequently confirmed Giovanni's Render workspace
`tea-d7kjsh3eo5us73dhcjrg`. The API is service `srv-d8mmv6urnols73cr5qa0`,
linked to this repository's main branch. Active deployment
`dep-damfk6ss728c73c12n5g` runs commit
`0dc752b28fb691cc2cc06afacbecab8fe7adefbd` from 18 September.

Read-only Neon verification at `2026-09-19T12:07:12.413Z` observed 23 applied
migrations, 75 public tables, five registered sources, 260 source records and
zero demographic series, releases or observations in project
`empty-cherry-93566793`, branch `br-morning-fire-as9l08lf`, database `neondb`.
Five successful acquisition records match the active Render process's commit,
source identities, counts and execution window
`2026-09-19T11:11:41.610Z`–`11:11:45.242Z`. This correlates the API process and
its ingestion database without exposing connection credentials. The original
10 September observation is retained separately in the ledger.

Two operational boundaries remain:

- Render reports that the workspace's build pipeline minutes are exhausted.
  Subsequent main deployments are cancelled, including
  `dep-dan753ff3r2c73dosf1g`. The scheduled [18 September source-sync check](https://github.com/colazeta/Lamezia-Trasparente-Monitor/actions/runs/35402896849)
  failed after 45 attempts with `DEPLOY_COMMIT_MISMATCH`, consistent with the
  older active deployment. Retrying a build does not resolve that capacity limit.
- The inspected public bundle `/assets/index-BBpZMfAe.js` compiles the public
  API-base resolver to null. The public same-origin readiness URL returns
  HTTP 503 with `scope: static-frontend` and `status: source-unavailable`.
  The owner-console's explicit Render-origin fallback does not configure public
  consumers. Configure the production build's `VITE_API_BASE_URL` and verify
  cross-origin public reads only after the new API/import is ready; no frontend
  endpoint switch is certified here.

No production import, deployment, spending-limit change or credential change
was performed. Successful build or preview checks alone do not satisfy these
remaining gates.

## Expanded-manifest verification repair

The production comparator still required exactly five sources, so it would
reject the reviewed eight-source plan before checking Render. Its existing
tests exercised only a synthetic five-source fixture. It now derives the exact
source set from the prepared plan, rejects missing/extra/duplicate sources, and
compares demographic series identity and expected, verified and inserted
observation counts. Expectations come from the existing source decoder, not a
second hard-coded mapping. The public report explicitly allows only those
aggregate expectation fields.

Regression tests use the actual committed eight-source plan. The PostgreSQL
integration workflow also feeds its real initial-import and rerun reports
through the production comparator, so database success and operational
verification are checked together. The strict deployed-commit check remains.

The repair passed whole-workspace typecheck/build, 88 database tests, seven
checkpoint tests, six ledger tests, migration safety and both architecture
audits locally. The real PostgreSQL report test is intentionally skipped
without its integration report; the updated remote integration workflow is
required to execute it before this repair is considered validated end to end.

## Remaining scope

- The distinct ISTAT household-composition benchmark for 2023 now has a [candidate common-schema adapter and primary consumer cutover](household-composition-canonicalisation.md). Its production gates and source materializer remain pending. Earlier eight-source receipts above are historical; the expanded candidate plans nine sources, 316 records and 75 typed observations.
- PNRR retains a file-backed frontend consumer alongside canonical projects.
- Albo, procurement, institutions, parties, geography, assets, remaining series,
  taxonomy and editorial-research collections retain their domain queues.
- No legacy writers have been retired and no production gate has been promoted
  to verified by this continuation.

The PR remains a reviewable candidate until the actual runtime and rollout path
are verified. Independent domain work remains possible; the runtime gate is not
a justification to label the entire remaining migration blocked.
