name: WhisperX PoC guards

on:
  workflow_dispatch:
  pull_request:
    paths:
      - '.github/workflows/whisperx-poc.yml'
      - 'tools/whisperx/**'
      - 'docs/architecture/whisperx-integration.md'

permissions:
  contents: read

concurrency:
  group: whisperx-poc-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  guards:
    name: Local-file / privacy / no-diarization guards
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run lightweight helper tests
        run: |
          python tools/whisperx/test_extract_media.py
          python tools/whisperx/test_benchmark_metrics.py
          python tools/whisperx/test_prepare_quality_review.py
          python -m py_compile \
            tools/whisperx/extract_media.py \
            tools/whisperx/benchmark_metrics.py \
            tools/whisperx/prepare_quality_review.py

      - name: Verify pinned upstream and corpus metadata
        shell: bash
        run: |
          set -euo pipefail
          grep -Fx 'whisperx==3.8.6' tools/whisperx/requirements.txt
          test "$(jq -r '.upstream.pinnedVersion' tools/whisperx/benchmark-corpus.public.json)" = '3.8.6'
          test "$(jq -r '.items[0].sourceRole' tools/whisperx/benchmark-corpus.public.json)" = 'benchmark-only-public-mirror'
          test "$(jq -r '.items[0].canonicalStatus' tools/whisperx/benchmark-corpus.public.json)" = 'not-established'
          test "$(jq -r '.processorPolicy.expectedVersion' tools/whisperx/lamezia-quality-plan.public.json)" = '3.8.6'
          test "$(jq -r '.processorPolicy.requiredLanguage' tools/whisperx/lamezia-quality-plan.public.json)" = 'it'
          test "$(jq -r '.processorPolicy.diarizationEnabled' tools/whisperx/lamezia-quality-plan.public.json)" = 'false'
          test "$(jq -r '.promotionRule.automaticPassPermitted' tools/whisperx/lamezia-quality-plan.public.json)" = 'false'

      - name: Verify no diarization or remote-fetch processor path
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          source = Path('tools/whisperx/extract_media.py').read_text(encoding='utf-8')
          forbidden = [
              'DiarizationPipeline',
              'assign_word_speakers',
              '--hf_token',
              '--hf-token',
              'requests.get(',
              'httpx.get(',
              'urllib.request',
          ]
          found = [item for item in forbidden if item in source]
          if found:
              raise SystemExit('WhisperX processor boundary violation: ' + ', '.join(found))

          review = Path('tools/whisperx/prepare_quality_review.py').read_text(encoding='utf-8')
          if 'pending-human-review' not in review or 'automaticPassPermitted' not in review:
              raise SystemExit('WhisperX quality gate lost its human-decision boundary')
          PY
